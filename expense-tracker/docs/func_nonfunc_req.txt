# Project Requirements Document

## Functional Requirements : This describes what the system must do.

1. Add Expenses
    - User can add an expense with:
        Title (String)
        Amount (positive number)
        Category (String) 
    - System must validate input before saving.
    - Expenses must be saved persistenetly to a file.
2. View All Expenses
    - User can view all stored expenses.
    - Expenses must be:
        Sorted by amount (decending).
        Displayed with index number.
3. Filter Expenses by Category
    - User can enter a Category.
    - System must display only matching expenses.
    - Category match must be case-insensitive.
    - Results must be stored by amount descending.
    - Empty category input must raise a validation error.
4. Delete Expenses
    - User can delete an expense using it index.
    - System must:
        Validate index range.
        Return error if invalid.
        Return error if index value is negative ot zero
        Return error if expense store is empty
5. Show Total Spending
    - System must calculate:
        Total sum of all expenses.
    - Must return numeric result.
    - Must return 0.00 if no expense exist.
6. Data Persistance
    - All expenses must be stored in:
        data/expense.json
    - Data must persist between application runs.
    - Application must handle corrupted JSON gracefully.
7. Logging
    - Application must log:
        Add operation (INFO)
        Delete operation (INFO)
        Invalid delete attempts (WARNINGS)
    - Log must be writen to: 
        logs/app.log
    - Log file must be created automatically on first run.
8. Input Validation
    - System must validate:
        Title cannot be empty
        Amount must:
            Be numeric.
            Be greater than zero.
        Category cannot be empty.
    - Invalid input must raise exceptions and not crash the application.
    - Specific error message must be returned:
      "Title cannot be empty"
      "Amount must be numeric"
      "Amount must be greater than zero"
      "Category cannot be empty"
      "Invalid index"
      "No expense found"


## Non-Functional Requirements : These describes how the system should behave.

1. Maintainability
    - Code must be modular.
    - Business logic must be separated from CLI logic.
    - Validation must be reusable across all features.
2. Testability
    - Core logic must be testable independently.
    - Unit tests must cover:
        Add expense
        Delete expense
        Total calculation
3 Relaibility
    - Application must handle:
        Corrupted JSON file
        Invalid numeric input
        Invaid delete index
    - Application must not crash on user error.
4. Performance
    - Application should handle atleast 1000 expenses without noticeable delay.
5. Usability
    - CLI menu must be:
        Clearly display options.
        Handle invalid menu input gracefully.
        Allow clean exist without errors.

## System Requirements
    - Python3.9+
    - No external libraries required
    - Workson macOS / Windows / Linux

## QA Acceptance Criteria
    Application is considered complete when:
        Adding valid expenses Works
        Adding invalid expenses shows correct error message
        Deleting valid index Works
        Deleting invalid index shows WARNINGS log
        Deleting negative or zero index shows WARNING log
        Total calculation is correct
        Total returns 0.00 when no expenses exist
        Data persists after restart
        Log file is generated automatically
        INFO log created on add and delete
        WARNING log created on invalid delete
        Unit test pass
        Applcation doesnot crash on any invalid input
        Corrupted JSON is handled gracefully

